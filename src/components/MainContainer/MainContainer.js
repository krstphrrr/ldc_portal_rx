import React from "react";

class MainContainer extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      menu: props.menu,
      mobileMenu: false,
    };
  }
  render() {
    return <div>o</div>;
  }
}

export default MainContainer;
